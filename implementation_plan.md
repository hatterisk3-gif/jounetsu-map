# 農業CADの幅調整と品種作型マスタ登録UIの改善

## Goal
農業CADでの間隔（幅）調整のアルゴリズム変更、および品種作型マスタ登録モーダルの入力UIとペイントグリッドをより詳細な半旬（1ヶ月を6分割）単位に変更します。

## Proposed Changes

### 1. 農業CAD機能（`cad.js`）
- **幅のみのスケール調整**: 現在の `cadAdjustRidgeGap(delta)` は `turf.transformScale` を使用して縦横両方を拡大縮小しています。これを修正し、ポリゴンの重心を基準として、`window.cadBaseAngle`（畝の並び方向＝幅方向）の軸上でのみ頂点座標をスケーリングする独自の幾何学計算処理に変更します。これにより、畝の長さは変わらず、間隔（幅）だけが変化するようになります。

### 2. 品種作型マスタ登録モーダル（`cultivation_modal.html`, `cultivation.js`）
- **作物のプルダウン化**:
  - `crCrop` を `<input type="text">` から `<select>` に変更します。
  - `cultivation.js` の `openCroptypeRegistrationModal()` にて、栽培計画側と同様にマスタデータから作物リストを取得してプルダウンの選択肢を自動生成する処理 (`populateSelect`) を追加します。
- **ペイントツールの半旬化 (108列グリッド)**:
  - `renderCroptypePaintGrid()` 関数を改修し、現在の「月ごと（18列）」から「半旬ごと（18ヶ月 × 6半旬 = 108列）」のグリッドを生成するように変更します。
  - 見出し行も栽培計画カレンダーと同様に「上前、上後、中前、中後、下前、下後」の表記を持つようにし、スクロール可能な形で表示します。

## User Review Required
> [!IMPORTANT]
> 農業CADの「幅だけを調整する」処理において、基準となる角度は「CADの設定画面で入力した角度（`cadBaseAngle`）」を使用します。もし手書きで自由に書いた四角形などの場合、その図形固有の向きではなく、画面上での全体設定角度を基準に幅が伸縮しますが問題ないでしょうか？

## Verification Plan
### Automated Tests
- 各JSファイルの構文チェック (`node -c`).
### Manual Verification
- CAD画面にて畝の間隔調整ボタンを押し、長さが変わらずに幅だけが変わるか確認。
- 作型マスタ登録モーダルを開き、作物がプルダウンで選べるか確認。
- ペイントツールが半旬単位（108列）でクリック＆ドラッグ（またはクリック）で色が塗れるか確認。
